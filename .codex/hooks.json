{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */CLAUDE.md|*/AGENTS.md|*/ktx.yaml|*/semantic-layer/*) jq -n --arg msg \"⚠️ 修改 $f 属于治理文件（CLAUDE.md/AGENTS.md/ktx.yaml/semantic-layer），按 docs/DEVELOPMENT.md 应先走 Plan Mode（EnterPlanMode）。\" '{systemMessage: $msg}' ;; esac; } 2>/dev/null"
          }
        ]
      }
    ]
  }
}
