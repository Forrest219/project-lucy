# project-lucy — Agent 开发入口

本仓库的开发治理规则（Plan Mode 强制流程、红线、Spec 落位规则）见 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)。

> `CLAUDE.md` 是 KTX 产品的数据问答运行时上下文（被 `ktx.yaml` 的 `llm.provider.backend: claude-code` 注入），不是开发说明，请不要在此查找开发规则。
