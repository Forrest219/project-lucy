# Demo Superstore

The Docker demo project contains a small `demo-mysql/superstore_orders` dataset.

Use `superstore_orders.total_sales` for revenue, `superstore_orders.total_profit`
for profit, and always apply `superstore_orders.active_rows` for ordinary
analysis.
