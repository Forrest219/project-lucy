# E2E fixture: finance_mart Schema Manifest
# 关联：E2E-PUB-01 / E2E-CON-03
# 此文件作为 E2E spec 的粘贴输入；可被 CatalogAssetUploadDrawer 解析。
# 真实表结构：fact_revenue / dim_account / dim_product

schema: finance_mart
version: 1
tables:
  - name: fact_revenue
    comment: "事实表：营收交易"
    columns:
      - name: order_id
        type: string
        pk: true
      - name: line_id
        type: int
        pk: true
      - name: account_id
        type: string
      - name: product_id
        type: string
      - name: revenue
        type: decimal
      - name: cost
        type: decimal
      - name: profit
        type: decimal
      - name: order_date
        type: date

  - name: dim_account
    comment: "维度表：客户账户"
    columns:
      - name: account_id
        type: string
        pk: true
      - name: account_name
        type: string
      - name: region
        type: string

  - name: dim_product
    comment: "维度表：产品"
    columns:
      - name: product_id
        type: string
        pk: true
      - name: product_name
        type: string
      - name: category
        type: string
