# src/

前端（React + Vite + TS）。
- `app/` 入口/路由/Provider/布局
- `pages/` Catalog / TableEditor / JoinEditor / WikiEditor / Review
- `components/` DiffViewer / YamlPreview / StatusBadge / shadcn 封装
- `lib/` apiClient（强制检查 error envelope, ADR-09）/ types / zod / queryKeys
